# Digit-Recognition-using-DL
Digit Recognition using CNN is a deep learning project that classifies handwritten digits (0–9) from images. The model is trained on labeled data to achieve accurate predictions, demonstrating the power of Convolutional Neural Networks in computer vision tasks.
